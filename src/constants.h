#pragma once

#include <cstdint>

const std::uint8_t EEPROM_ADDR_SENSOR_TYPE = 5;

// Sensors 
const std::uint8_t SENSOR_TYPE_DS18B20 = 2;

// Sleep behavior
const std::uint8_t SLEEP_TYPE_INTERVAL = 2;